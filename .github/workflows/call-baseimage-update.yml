name: Check for base image updates
on:
  workflow_dispatch:
  schedule:
    - cron:  '0 0 * * 0'

jobs:
  call-workflow:
    uses: thespad/actions/.github/workflows/check-baseimage-update.yml@v9.0.0
    with:
      repo_owner: ${{ github.repository_owner }}
      baseimage: "alpine"
      basebranch: "3.22"
      app_name: "whisparr"
      prerelease: true
      branch: v3
    secrets:
      repo_scoped_token: ${{ secrets.repo_scoped_token }}
